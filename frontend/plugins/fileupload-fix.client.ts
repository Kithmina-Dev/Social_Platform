import { defineNuxtPlugin } from "#app";
// This plugin is now deprecated as FileUpload is registered in primevue.client.ts
// Keeping this file to avoid breaking changes

export default defineNuxtPlugin((nuxtApp) => {
  // FileUpload is already registered in primevue.client.ts
});
